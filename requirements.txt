# Core server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pyyaml>=6.0.1

# LLM providers - only the one selected via `llm_provider` in config.yaml
# actually needs to be installed at runtime, but all are listed so any of
# them works out of the box. DeepSeek's API is OpenAI-compatible, so it
# reuses the `openai` package (no separate dependency needed).
openai>=1.30.0
anthropic>=0.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
